<clickhouse>
    <!-- Allow connections from all interfaces so Docker's port-proxy can forward
         traffic from the host. The default config only binds to ::1 and 127.0.0.1
         (loopback), which is unreachable via Docker's veth-based port mapping. -->
    <listen_host>0.0.0.0</listen_host>
</clickhouse>
